# # fastapi>=0.109.0
# # uvicorn[standard]>=0.27.0
# # python-multipart>=0.0.9
# # pillow>=10.2.0
# # numpy>=1.26.4
# # tensorflow-cpu>=2.16.1
# # torch>=2.2.0
# # torchvision>=0.17.0
# fastapi==0.110.0
# uvicorn[standard]==0.27.1
# python-multipart==0.0.9
# pillow==10.2.0
# numpy==1.23.5
# tensorflow-cpu==2.13.0
# torch==2.0.1
# torchvision==0.15.2# ─────────────────────────────────────────────────────────
# Errorbite CancerScreen — ONNX Runtime version
# Much lighter than TF + PyTorch combined!
#
# OLD stack RAM on Railway:  ~1.2 GB (TF + PyTorch both loaded)
# NEW stack RAM on Railway:  ~180 MB (onnxruntime only)
# ─────────────────────────────────────────────────────────

fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9
pillow==10.2.0
numpy==1.24.4
onnxruntime==1.17.3
psutil==5.9.8

# ─────────────────────────────────────────────────────────
# ONE-TIME conversion tools (run locally, NOT on Railway)
# Install these on your laptop to convert models to .onnx
# ─────────────────────────────────────────────────────────
# tf2onnx>=1.16.1          # converts .keras → .onnx
# torch>=2.0.1             # converts .pth → .onnx
# torchvision>=0.15.2